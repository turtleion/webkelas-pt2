# Android build artefacts
android/app/build/
android/.gradle/
android/build/

# Local keystores & credentials
android/release.keystore
android/keystore.properties
keys/*.keystore

# Environment files (contains Google client ID)
.env.local

# Node modules (re‑generated)
node_modules/

# Optional lockfile (you may keep locally)
pnpm-lock.yaml

# Build output
dist
tkj1smknice-android-*.apk

# Android keystores (sensitive)
android/keystores/*.keystore
android/keystore.properties

# Tool/agent dirs — bukan bagian repo
.agents/
.claude/
.convex/
.netlify/
graphify-out/cache/
supabase/.temp/